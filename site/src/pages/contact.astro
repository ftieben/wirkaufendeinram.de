---
import BaseLayout from '../layouts/BaseLayout.astro';
---

<BaseLayout 
  title="Kontakt - Hilfe beim RAM-Verkauf | RAM Ankauf"
  description="Kontaktieren Sie RAM Ankauf für Hilfe bei der Arbeitsspeicher-Bewertung, Angeboten, Versand oder bei Fragen zu unseren RAM-Ankaufsdiensten."
>
  <!-- Hero Section -->
  <section class="contact-hero">
    <div class="hero-content">
      <h1>Kontaktieren Sie unsere RAM-Spezialisten</h1>
      <p class="hero-subtitle">
        Erhalten Sie Expertenhilfe bei Ihrer Arbeitsspeicher-Bewertung und Ihrem Verkauf. Unser Team steht Ihnen zur Verfügung.
      </p>
      <div class="parody-disclaimer">
        <span class="disclaimer-icon">🎭</span>
        <span class="disclaimer-text">Parodie-Konzept, echter Service - wir kaufen tatsächlich Ihr RAM!</span>
      </div>
    </div>
  </section>

  <!-- Contact Methods -->
  <section class="contact-methods">
    <div class="container">
      <h2 class="section-title">Kontaktieren Sie uns</h2>
      <div class="methods-grid">
        <div class="contact-card primary">
          <div class="contact-icon">📞</div>
          <h3 class="contact-title">Telefon-Support</h3>
          <p class="contact-description">
            Sprechen Sie direkt mit unseren RAM-Spezialisten für sofortige Hilfe bei Angeboten und technischen Fragen.
          </p>
          <a href="tel:+49-555-RAM-KAUF" class="contact-action">Anrufen (555) RAM-KAUF</a>
          <div class="contact-hours">
            <strong>Öffnungszeiten:</strong> Montag-Freitag, 9:00 - 18:00 Uhr
          </div>
        </div>
        
        <div class="contact-card">
          <div class="contact-icon">✉️</div>
          <h3 class="contact-title">E-Mail-Support</h3>
          <p class="contact-description">
            Senden Sie uns detaillierte Fragen zu Ihren RAM-Modulen oder erhalten Sie Hilfe beim Bewertungsprozess.
          </p>
          <a href="mailto:support@ramankauf.de" class="contact-action">E-Mail senden</a>
          <div class="contact-hours">
            <strong>Antwort:</strong> Innerhalb von 4 Stunden an Werktagen
          </div>
        </div>
        
        <div class="contact-card">
          <div class="contact-icon">💬</div>
          <h3 class="contact-title">Live-Chat</h3>
          <p class="contact-description">
            Erhalten Sie sofortige Hilfe während der Geschäftszeiten mit unserem Live-Chat-Support-System.
          </p>
          <button class="contact-action chat-button">Live-Chat starten</button>
          <div class="contact-hours">
            <strong>Verfügbar:</strong> Montag-Freitag, 9:00 - 18:00 Uhr
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Contact Form -->
  <section class="contact-form-section">
    <div class="container">
      <div class="form-container">
        <div class="form-header">
          <h2 class="form-title">Senden Sie uns eine Nachricht</h2>
          <p class="form-description">
            Haben Sie eine spezifische Frage oder benötigen Sie detaillierte Hilfe? Füllen Sie das untenstehende Formular aus und wir melden uns umgehend bei Ihnen.
          </p>
        </div>
        
        <form class="contact-form" action="#" method="post">
          <div class="form-row">
            <div class="form-group">
              <label for="firstName" class="form-label">Vorname *</label>
              <input 
                type="text" 
                id="firstName" 
                name="firstName" 
                class="form-input" 
                required 
                aria-describedby="firstName-error"
              >
              <div id="firstName-error" class="error-message" role="alert"></div>
            </div>
            
            <div class="form-group">
              <label for="lastName" class="form-label">Nachname *</label>
              <input 
                type="text" 
                id="lastName" 
                name="lastName" 
                class="form-input" 
                required 
                aria-describedby="lastName-error"
              >
              <div id="lastName-error" class="error-message" role="alert"></div>
            </div>
          </div>
          
          <div class="form-row">
            <div class="form-group">
              <label for="email" class="form-label">E-Mail-Adresse *</label>
              <input 
                type="email" 
                id="email" 
                name="email" 
                class="form-input" 
                required 
                aria-describedby="email-error"
              >
              <div id="email-error" class="error-message" role="alert"></div>
            </div>
            
            <div class="form-group">
              <label for="phone" class="form-label">Telefonnummer</label>
              <input 
                type="tel" 
                id="phone" 
                name="phone" 
                class="form-input" 
                aria-describedby="phone-error"
              >
              <div id="phone-error" class="error-message" role="alert"></div>
            </div>
          </div>
          
          <div class="form-group">
            <label for="subject" class="form-label">Betreff *</label>
            <select id="subject" name="subject" class="form-select" required aria-describedby="subject-error">
              <option value="">Thema auswählen...</option>
              <option value="quote">RAM-Angebotsfrage</option>
              <option value="technical">Technischer Support</option>
              <option value="shipping">Versand & Logistik</option>
              <option value="payment">Zahlung & Abrechnung</option>
              <option value="general">Allgemeine Anfrage</option>
              <option value="other">Sonstiges</option>
            </select>
            <div id="subject-error" class="error-message" role="alert"></div>
          </div>
          
          <div class="form-group">
            <label for="message" class="form-label">Nachricht *</label>
            <textarea 
              id="message" 
              name="message" 
              class="form-textarea" 
              rows="6" 
              required 
              placeholder="Bitte geben Sie Details zu Ihrer Frage oder den RAM-Modulen an, die Sie verkaufen möchten..."
              aria-describedby="message-error"
            ></textarea>
            <div id="message-error" class="error-message" role="alert"></div>
          </div>
          
          <div class="form-group">
            <label class="checkbox-label">
              <input type="checkbox" name="newsletter" class="form-checkbox">
              <span class="checkbox-text">
                Ich möchte Updates über RAM-Markttrends und Preise erhalten (optional)
              </span>
            </label>
          </div>
          
          <button type="submit" class="form-submit">Nachricht senden</button>
        </form>
      </div>
    </div>
  </section>

  <!-- Additional Info -->
  <section class="additional-info">
    <div class="container">
      <div class="info-grid">
        <div class="info-card">
          <h3 class="info-title">Geschäftsadresse</h3>
          <div class="info-content">
            <p>RAM Ankauf Hauptsitz<br>
            Speicherstraße 123<br>
            12345 Tech-Stadt<br>
            Deutschland</p>
          </div>
        </div>
        
        <div class="info-card">
          <h3 class="info-title">Versandadresse</h3>
          <div class="info-content">
            <p>Für den Versand von RAM-Modulen:<br>
            RAM Ankauf Verarbeitungszentrum<br>
            Hardware-Boulevard 456<br>
            12345 Tech-Stadt<br>
            Deutschland</p>
          </div>
        </div>
        
        <div class="info-card">
          <h3 class="info-title">Antwortzeiten</h3>
          <div class="info-content">
            <ul>
              <li><strong>Telefon:</strong> Sofort während der Geschäftszeiten</li>
              <li><strong>Live-Chat:</strong> Unter 2 Minuten</li>
              <li><strong>E-Mail:</strong> Innerhalb von 4 Stunden</li>
              <li><strong>Kontaktformular:</strong> Innerhalb von 24 Stunden</li>
            </ul>
          </div>
        </div>
      </div>
    </div>
  </section>
</BaseLayout>

<style>
  /* Hero Section */
  .contact-hero {
    background: linear-gradient(135deg, #2d3748 0%, #1a202c 100%);
    color: white;
    padding: 4rem 0;
    text-align: center;
  }

  .hero-content {
    max-width: 800px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  .hero-content h1 {
    font-size: 3rem;
    font-weight: 700;
    margin-bottom: 1rem;
    color: white;
  }

  .hero-subtitle {
    font-size: 1.25rem;
    opacity: 0.95;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .parody-disclaimer {
    background: rgba(255, 255, 255, 0.15);
    padding: 0.75rem 1.5rem;
    border-radius: 25px;
    backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.2);
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    font-size: 0.875rem;
    color: rgba(255, 255, 255, 0.9);
  }

  .disclaimer-icon {
    font-size: 1rem;
  }

  /* Container */
  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
  }

  /* Section Titles */
  .section-title {
    text-align: center;
    font-size: 2.5rem;
    margin-bottom: 3rem;
    color: #1a365d;
  }

  /* Contact Methods */
  .contact-methods {
    padding: 4rem 0;
    background: white;
  }

  .methods-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .contact-card {
    background: #f7fafc;
    padding: 2rem;
    border-radius: 12px;
    border: 1px solid #e2e8f0;
    text-align: center;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .contact-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 30px rgba(0, 0, 0, 0.15);
  }

  .contact-card.primary {
    background: linear-gradient(135deg, #3182ce 0%, #2c5aa0 100%);
    color: white;
    border-color: #2c5aa0;
  }

  .contact-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
    display: block;
  }

  .contact-title {
    font-size: 1.5rem;
    margin-bottom: 1rem;
    color: #2d3748;
  }

  .contact-card.primary .contact-title {
    color: white;
  }

  .contact-description {
    color: #4a5568;
    margin-bottom: 1.5rem;
    line-height: 1.6;
  }

  .contact-card.primary .contact-description {
    color: rgba(255, 255, 255, 0.9);
  }

  .contact-action {
    display: inline-block;
    background: #3182ce;
    color: white;
    padding: 0.75rem 1.5rem;
    border-radius: 6px;
    text-decoration: none;
    font-weight: 600;
    margin-bottom: 1rem;
    transition: background-color 0.2s ease;
    border: none;
    cursor: pointer;
    font-size: 1rem;
  }

  .contact-action:hover {
    background: #2c5aa0;
    color: white;
  }

  .contact-card.primary .contact-action {
    background: white;
    color: #3182ce;
  }

  .contact-card.primary .contact-action:hover {
    background: #f7fafc;
    color: #2c5aa0;
  }

  .contact-hours {
    font-size: 0.875rem;
    color: #718096;
  }

  .contact-card.primary .contact-hours {
    color: rgba(255, 255, 255, 0.8);
  }

  /* Contact Form Section */
  .contact-form-section {
    padding: 4rem 0;
    background: #f7fafc;
  }

  .form-container {
    max-width: 800px;
    margin: 0 auto;
    background: white;
    padding: 3rem;
    border-radius: 12px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  }

  .form-header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .form-title {
    font-size: 2rem;
    margin-bottom: 1rem;
    color: #1a365d;
  }

  .form-description {
    color: #4a5568;
    line-height: 1.6;
    margin: 0;
  }

  /* Form Styles */
  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1.5rem;
  }

  .form-row {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1.5rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.5rem;
  }

  .form-label {
    font-weight: 500;
    color: #2d3748;
    font-size: 0.875rem;
  }

  .form-input,
  .form-select,
  .form-textarea {
    padding: 0.75rem;
    border: 1px solid #e2e8f0;
    border-radius: 6px;
    font-size: 1rem;
    transition: border-color 0.2s ease, box-shadow 0.2s ease;
  }

  .form-input:focus,
  .form-select:focus,
  .form-textarea:focus {
    outline: none;
    border-color: #3182ce;
    box-shadow: 0 0 0 3px rgba(49, 130, 206, 0.1);
  }

  .form-textarea {
    resize: vertical;
    min-height: 120px;
  }

  .checkbox-label {
    display: flex;
    align-items: flex-start;
    gap: 0.75rem;
    cursor: pointer;
  }

  .form-checkbox {
    margin-top: 0.25rem;
  }

  .checkbox-text {
    color: #4a5568;
    line-height: 1.5;
  }

  .form-submit {
    background: #3182ce;
    color: white;
    padding: 1rem 2rem;
    border: none;
    border-radius: 6px;
    font-size: 1.125rem;
    font-weight: 600;
    cursor: pointer;
    transition: background-color 0.2s ease;
    align-self: flex-start;
  }

  .form-submit:hover {
    background: #2c5aa0;
  }

  .error-message {
    color: #e53e3e;
    font-size: 0.875rem;
    min-height: 1.25rem;
  }

  /* Additional Info */
  .additional-info {
    padding: 4rem 0;
    background: white;
  }

  .info-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
  }

  .info-card {
    background: #f7fafc;
    padding: 2rem;
    border-radius: 8px;
    border: 1px solid #e2e8f0;
  }

  .info-title {
    font-size: 1.25rem;
    margin-bottom: 1rem;
    color: #2d3748;
  }

  .info-content p {
    color: #4a5568;
    line-height: 1.6;
    margin: 0;
  }

  .info-content ul {
    list-style: none;
    padding: 0;
    margin: 0;
  }

  .info-content li {
    padding: 0.25rem 0;
    color: #4a5568;
  }

  /* Responsive Design */
  @media (max-width: 768px) {
    .hero-content h1 {
      font-size: 2.25rem;
    }

    .hero-subtitle {
      font-size: 1.125rem;
    }

    .section-title {
      font-size: 2rem;
    }

    .methods-grid {
      grid-template-columns: 1fr;
    }

    .form-container {
      padding: 2rem;
    }

    .form-row {
      grid-template-columns: 1fr;
    }

    .info-grid {
      grid-template-columns: 1fr;
    }

    .container {
      padding: 0 1rem;
    }
  }

  @media (max-width: 480px) {
    .hero-content {
      padding: 0 1rem;
    }

    .hero-content h1 {
      font-size: 1.875rem;
    }

    .section-title {
      font-size: 1.75rem;
    }

    .form-container {
      padding: 1.5rem;
    }

    .contact-card {
      padding: 1.5rem;
    }
  }

  /* Accessibility */
  @media (prefers-reduced-motion: reduce) {
    .contact-card,
    .contact-action,
    .form-input,
    .form-select,
    .form-textarea,
    .form-submit {
      transition: none;
    }

    .contact-card:hover {
      transform: none;
    }
  }

  @media (prefers-contrast: high) {
    .contact-card,
    .form-container,
    .info-card {
      border-width: 2px;
    }
  }
</style>

<script>
  // Form validation and submission
  document.addEventListener('DOMContentLoaded', function() {
    const form = document.querySelector('.contact-form') as HTMLFormElement;
    const chatButton = document.querySelector('.chat-button') as HTMLButtonElement;
    
    if (form) {
      form.addEventListener('submit', function(e) {
        e.preventDefault();
        
        // Basic form validation
        const requiredFields = form.querySelectorAll('[required]');
        let isValid = true;
        
        requiredFields.forEach(field => {
          const input = field as HTMLInputElement;
          const errorElement = document.getElementById(input.name + '-error');
          
          if (!input.value.trim()) {
            if (errorElement) {
              errorElement.textContent = 'Dieses Feld ist erforderlich';
            }
            isValid = false;
          } else {
            if (errorElement) {
              errorElement.textContent = '';
            }
          }
        });
        
        // Email validation
        const emailInput = form.querySelector('#email') as HTMLInputElement;
        const emailError = document.getElementById('email-error');
        if (emailInput && emailInput.value) {
          const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
          if (!emailRegex.test(emailInput.value)) {
            if (emailError) {
              emailError.textContent = 'Bitte geben Sie eine gültige E-Mail-Adresse ein';
            }
            isValid = false;
          }
        }
        
        if (isValid) {
          // In a real application, this would submit to a server
          alert('Vielen Dank für Ihre Nachricht! Wir melden uns innerhalb von 24 Stunden bei Ihnen.');
          form.reset();
        }
      });
    }
    
    // Chat button functionality
    if (chatButton) {
      chatButton.addEventListener('click', function() {
        // In a real application, this would open a chat widget
        alert('Live-Chat ist derzeit nicht verfügbar. Bitte rufen Sie uns unter (555) RAM-KAUF an oder senden Sie eine E-Mail für sofortige Hilfe.');
      });
    }
  });
</script>